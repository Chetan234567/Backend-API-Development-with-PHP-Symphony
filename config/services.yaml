# config/services.yaml
parameters:
   profile_picture_dir: '%kernel.project_dir%/public/uploads/profile'
   profile_picture_base_url: '/uploads/profile'
services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true
        autoconfigure: true

    # makes classes in src/ available to be used as services
    App\:
        resource: '../src/*'
        exclude: '../src/{DependencyInjection,Entity,Migrations,Tests,Kernel.php}'

    # explicitly register controllers
    App\Controller\:
        resource: '../src/Controller/'
        tags: ['controller.service_arguments']

    # specific controller service
    App\Controller\EmailVerificationController:
        public: true
        autowire: true
        tags: ['controller.service_arguments']
    App\Service\VideoService:
        arguments:
            $kernelProjectDir: '%kernel.project_dir%'